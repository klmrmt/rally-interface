# Rally Interface Environment Variables
# Copy this file to .env and fill in values.

# API base URL — in development, Vite proxies /rally-api to localhost:3000.
# In production, set this to the full backend URL (e.g. https://api.yoursite.com/rally-api).
VITE_API_BASE=/rally-api
